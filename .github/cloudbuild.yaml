# # TODO: Add FastAPI support and change to support serving inference requests instead of train
# steps:
# # Build the container image
# - name: 'gcr.io/cloud-builders/docker'
#   args: ['build', '-t', 'gcr.io/${{ secrets.GCP_PROJECT_NAME }}/gcp_train_image:latest', '.'] 
# # Push the container image to Container Registry
# - name: 'gcr.io/cloud-builders/docker'
#   args: ['push', 'gcr.io/${{ secrets.GCP_PROJECT_NAME }}/gcp_train_image:latest']
# # Deploy container image to Cloud Run
# - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
#   entrypoint: gcloud
#   args:
#   - 'run'
#   - 'deploy'
#   - '<service-name>'
#   - '--image'
#   - 'gcr.io/${{ secrets.GCP_PROJECT_NAME }}/gcp_train_image:latest'
#   - '--region'
#   - 'europe-north1'